# b1.Managed 类导航

## 命名空间清单

| 命名空间 | 说明 |
|----------|------|
| `b1` | 包含模块引用维护类 `ModuleRefMain`，用于 IL2CPP 链接时防止类型裁减 |

## 类/接口/结构/枚举清单

### `b1.ModuleRefMain`

| 条目 | 值 |
|------|-----|
| **完整类型名称** | `b1.ModuleRefMain` |
| **类型** | class |
| **继承关系** | 基类: `UObject` (Unreal Engine 对象基类) |
| **实现的接口** | `IAssemblyRef_GSECore`, `IAssemblyRef_BtlSvrMain` |
| **UObject 标记属性** | `[UClass]`, `[Blueprintable]`, `[BlueprintType]` |
| **简短中文说明** | 模块引用主类。通过 `FakeIL2CPPRef()` 静态方法创建大量 Unreal Engine 对象实例和泛型容器实例（存储于 `FakeObjs` 静态列表中），以强制 IL2CPP 链接器在预编译阶段保留这些类型的元数据，防止因代码裁剪导致运行时类型缺失。这是 UE + IL2CPP 项目中防止类型被 strip 的标准做法。 |
| **所在文件路径** | `/mnt/a/Game-A11y/BlackMyth-A11y/CSharpMods/B1CSharpLoader/GameDll-src/b1.Managed/ModuleRefMain.cs` |

#### 成员一览

| 成员 | 类别 | 签名字面 | 说明 |
|------|------|----------|------|
| `FakeObjs` | 静态字段 (private) | `private static List<object> FakeObjs` | 存储 FakeIL2CPPRef 中创建的所有对象实例的静态列表，用于确保类型引用不被裁剪 |
| `FakeIL2CPPRef()` | 静态方法 (public) | `public static void FakeIL2CPPRef()` | 实例化海量 UE 类型、泛型容器、值类型封送辅助类等，保证 IL2CPP 链接时保留其元数据。方法体被 `if (false)` 包裹，运行时永不执行，仅为编译期引用而存在 |

---

### 其他文件

| 文件路径 | 定义的内容 |
|----------|------------|
| `Properties/AssemblyInfo.cs` | 仅包含程序集级别特性（`AssemblyVersion("0.0.0.0")`、`SecurityPermission`），未定义任何类型 |

---

**总结：** 本项目 `b1.Managed` 仅包含 **1 个命名空间**（`b1`）和 **1 个类型定义**（`b1.ModuleRefMain` 类）。`AssemblyInfo.cs` 仅提供程序集元数据，不定义任何类型。
